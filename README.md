
  # Landing Page for BloodSync

  This is a code bundle for Landing Page for BloodSync. The original project is available at https://www.figma.com/design/V3iQ46oDEnBeNtvyzGbOam/Landing-Page-for-BloodSync.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  